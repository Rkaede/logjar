{
  "name": "logjar-extension",
  "version": "1.0.0",
  "private": true,
  "description": "Optional Chrome extension frontend capture for Logjar.",
  "type": "module",
  "scripts": {
    "build": "vp pack src/background.ts -f iife -d dist && vp pack src/content.ts -f iife -d dist --no-clean && vp pack src/page-bridge.ts -f iife -d dist --no-clean && vp pack src/popup.ts -f iife -d dist --no-clean && node ./scripts/copy-static.mjs",
    "dev": "vp run build",
    "test": "vp test",
    "check": "vp check",
    "prepare": "vp config"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "vite-plus": "catalog:"
  },
  "packageManager": "pnpm@10.33.0"
}
